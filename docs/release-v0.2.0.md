# ASN v0.2.0

Primera versión distribuible del agente ASN con integración MCP para Codex,
Claude Code y OpenCode.

Incluye los ejecutables `asn`, `asn-mcp` y `asn-agent-mcp`, además de `asn
setup --repo . --clients all` para integrar skills sin modificar el `Justfile`.
